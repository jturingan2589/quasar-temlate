export interface StoreFilter {
  id: number;
  store_name: string;
}

export interface CategoryFiler {
  id: string;
  category_name: string;
}

export interface BrandFilter {
  id: string;
  category_name: string;
}
